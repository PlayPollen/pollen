# @colyseus/core declares optional integrations (Redis presence/driver,
# uWebSockets transport) as peers. We deliberately use none of them yet — the
# server picks its transport explicitly in server/src/main.ts — so we don't want
# npm auto-installing them, least of all the native uWebSockets build.
legacy-peer-deps=true
